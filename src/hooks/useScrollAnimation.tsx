import React, { useLayoutEffect, useRef, useState } from "react";

function useScrollAnimation(element: JSX.Element | JSX.Element[]) {
  console.log("This is the element received: " + element);

}

export default useScrollAnimation;